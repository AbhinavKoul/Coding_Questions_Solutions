#include<iostream>
#include<string>
#include<unordered_map>
#define ul unordered_map

using namespace std;

bool checkOneOdd_inMap(ul<int,int> map)
{
    bool found_odd = false;
    for(auto i:map.)
    {

    }
}

bool checkPalindromePossible(string str)
{
    int count_odd;
    ul<int,int> char_counter;
    for(auto i : str)
        char_counter[i]++;
    
    //check only one odd value in the table
    result = checkOneOdd_inMap(char_counter);
    

}